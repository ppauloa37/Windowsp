function setup() {
  let canvas = createCanvas(800, 600);
  canvas.parent('canvas');
  background(240);
}

function draw() {
  // Lógica do sistema operacional
  fill(0);
  textSize(16);
  text('Bem-vindo ao Sistema Operacional Simulado!', 50, 50);
}
